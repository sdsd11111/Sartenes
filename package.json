{
  "name": "los-sartenes",
  "version": "1.0.0",
  "description": "Sistema de gestión de menú para Los Sartenes",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "echo 'Build step started...' && mkdir -p public && cp -r components/ public/ && cp -r images/ public/ && cp -r admin/ public/ && cp -r css/ public/ && cp -r js/ public/ && cp *.html public/ && cp -r menu/ public/ && cp favicon.ico public/ 2>/dev/null || true && cp robots.txt public/ 2>/dev/null || true && cp sitemap.xml public/ 2>/dev/null || true",
    "vercel-build": "npm install && npm run build",
    "migrate": "mysql -u root -p < database/migrations/001_create_platos_del_dia.sql",
    "setup": "npm install && npm run migrate"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "restaurant",
    "menu",
    "management",
    "nodejs",
    "express",
    "mysql"
  ],
  "author": "",
  "license": "ISC"
}
