# ======================================
# Configuración de la Aplicación - Los Sartenes
# ======================================
# Copia este archivo a .env y ajusta los valores según sea necesario

# ====================
# Entorno de Ejecución
# ====================
NODE_ENV=development                   # Entorno de ejecución (development, production, test)
PORT=3000                              # Puerto en el que se ejecutará el servidor
HOST=0.0.0.0                           # Host en el que se ejecutará el servidor

# ====================
# Configuración de Supabase
# ====================
SUPABASE_URL=tu_url_de_supabase        # URL de tu proyecto Supabase
SUPABASE_ANON_KEY=tu_clave_anonima     # Clave anónima de Supabase
SUPABASE_SERVICE_ROLE_KEY=tu_clave_de_servicio  # Clave de servicio de Supabase (solo para operaciones del lado del servidor)

# ====================
# Seguridad
# ====================
SESSION_SECRET=clave_secreta_muy_larga_y_segura  # Usado para firmar las cookies de sesión
JWT_SECRET=clave_secreta_para_jwt                # Usado para firmar tokens JWT
JWT_EXPIRES_IN=7d                                # Tiempo de expiración de los tokens JWT (ej: 15m, 1h, 7d)
PASSWORD_SALT_ROUNDS=10                          # Número de rondas de sal para el hashing de contraseñas

# ====================
# Configuración de CORS
# ====================
CORS_ORIGIN=http://localhost:3000,http://localhost:3001  # Orígenes permitidos (separados por comas)
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS                # Métodos HTTP permitidos
CORS_ALLOWED_HEADERS=Content-Type,Authorization          # Cabeceras permitidas

# ====================
# Subida de Archivos
# ====================
UPLOAD_DIR=./public/uploads                     # Directorio donde se guardarán las imágenes subidas
MAX_FILE_SIZE=10MB                               # Tamaño máximo de archivo permitido
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp  # Tipos MIME permitidos

# ====================
# Correo Electrónico (SMTP)
# ====================
SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=tu_usuario
SMTP_PASS=tu_contraseña
SMTP_FROM=noreply@tudominio.com
SMTP_SECURE=false  # true para puerto 465, false para otros puertos

# ====================
# Configuración de la Aplicación
# ====================
APP_NAME="Los Sartenes"
APP_URL=http://localhost:3000
ADMIN_EMAIL=admin@tudominio.com
CONTACT_EMAIL=contacto@tudominio.com

# ====================
# Configuración de Registro (Logging)
# ====================
LOG_LEVEL=info  # Nivel de registro: error, warn, info, debug, silly
LOG_FILE=logs/app.log  # Archivo de registro (dejar vacío para solo consola)

# ====================
# Características
# ====================
FEATURE_REGISTRATION=true    # Habilitar registro de nuevos usuarios
FEATURE_EMAIL_VERIFICATION=true  # Requerir verificación de correo electrónico
FEATURE_PASSWORD_RESET=true  # Habilitar restablecimiento de contraseña
FEATURE_MAINTENANCE=false    # Modo mantenimiento (mantendrá la aplicación fuera de línea)

# ====================
# Límites de Tasa (Rate Limiting)
# ====================
RATE_LIMIT_WINDOW_MS=900000  # Ventana de tiempo para el límite de tasa (15 minutos)
RATE_LIMIT_MAX=100           # Número máximo de solicitudes por ventana

# ====================
# Configuración de la Base de Datos (solo si no se usa Supabase)
# ====================
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=tu_contraseña
# DB_NAME=los_sartenes_db
# DB_SSL=false

# ====================
# Configuración de Redis (opcional, para sesiones o caché)
# ====================
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0
